# Degrees - Shortest Path Finding using BFS

This project investigates the "degrees of separation" between two actors by finding the shortest path through a movie database.

## Algorithm
Uses Breadth-First Search (BFS) to find the shortest path between actors through movies.

## How to Run
```bash
python degrees.py