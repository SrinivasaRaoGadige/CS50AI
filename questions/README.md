# Questions - QA System with TF-IDF

This project implements a question answering system using TF-IDF for document retrieval.

## Algorithm
Uses TF-IDF to rank documents by relevance to query questions.
